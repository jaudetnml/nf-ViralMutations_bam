{
    "$schema": "https://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/phac-nml/nf-ViralMutations/main/assets/schema_input.json",
    "title": "Samplesheet schema validation",
    "description": "Schema for the file provided with params.input",
    "type": "array",
    "items": {
        "type": "object",
        "properties": {
            "sample": {
                "type": "string",
                "pattern": "^\\S+$",
                "meta": ["external_id"],
                "errorMessage": "Sample name to be used in report generation. Valid characters include alphanumeric and -. All other characters will be replaced by underscores."
            },
            "sample_name": {
                "type": "string",
                "errorMessage": "Optional. Used to override sample when used in tools like IRIDA-Next. Valid characters include alphanumeric and -. All other characters will be replaced by underscores.",
                "meta": ["id"]
            },
            "bam_1": {
                "type": "string",
                "pattern": "^\\S+\\.f(ast)?q(\\.gz)?$",
                "format": "file-path",
                "errorMessage": "FastQ file for reads 1 (forward reads) must be provided, cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'. If this is meant to be a run of nf-ViralMuations on MinION data, leave set the directory or fastq file under `long_reads`.",
                "dependentRequired": ["bam_2"],
                "meta": ["bam_1"],
                "unique": true
            },
            "bam_2": {
                "type": "string",
                "pattern": "^\\S+\\.f(ast)?q(\\.gz)?$",
                "format": "file-path",
                "errorMessage": "FastQ file for reads 2 (reverse reads) cannot contain spaces and must have extension '.fq.gz' or '.fastq.gz'",
                "meta": ["bam_2"],
                "unique": true
            }
        },
        "required": ["sample", "bam_1", "bam_2"]
    }
}
